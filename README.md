# MyTerraform
This a terraform file for the purpose of a Dev enviornment needed for azure work. 

One of the things you will need is to generate an ssh key to connect to vm. (ssh-keygen -t rsa) command. Which is then saved to your local computer
Test